{# Company Space #}
<div class="container text-center">
	<div class="row justify-content-center mt-2 mb-5" id="navigation-btn-desktop">
		<div class="col mt-4">
			<a class="btn" role="button" id="navbtn5" href="{{ path('company_offers', { id: app.user.company.id }) }}">
				<h4>{{company.name|capitalize}} JOB OFFERS</h4>
				<p>Create new offers, find candidates list by offer and update the offers status.</p>
			</a>
		</div>
		<div class="col mt-4">
			<a class="btn" role="button" id="navbtn6" href="{{ path('company_edit', { id: company.id }) }}">
				<h4>PROFILE</h4>
				<p>Update your information to increase your chances to increase applications.</p>
			</a>
		</div>
	</div>
</div>
<script>
let endURL = window.location.href;
let lastElement = endURL.split('/').filter(element => element !== '').pop();
if (lastElement === 'offers') {
	document.getElementById('navbtn5').style.backgroundColor = '#FED32C';
} else {
	document.getElementById('navbtn6').style.backgroundColor = '#FED32C';
}
</script>
