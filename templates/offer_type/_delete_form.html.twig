<form method="post" action="{{ path('offer_form_delete', {'id': offer.id}) }}">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
	<button class="btn btn-primary">Delete Offer 👍🏽</button>
</form>
