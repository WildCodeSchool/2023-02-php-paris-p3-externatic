{% extends 'base.html.twig' %}

{% block title %}Edit Offer : {{ offer.title }}{% endblock %}

{% block body %}
    {% include "company/_dashboard.html.twig" %}
    {% include "company/_navigationBtnMobileCompany.html.twig" %}
	{% include "company/_navigationBtnDesktopCompany.html.twig" %}
    <div class="text-center mt-4 mb-4">
        <h1>Edit Offer : {{ offer.title }}</h1>
    </div>
    {% include "offer_type/_form.html.twig" %}
    <div class="text-center">
        <button type="submit" class="btn btn-secondary mt-4 bt-4">{{ button_label|default('Edit') }}</button>
    </div>
    {{ form_end(form) }}
    <div class="text-center">
        <button type="submit" data-bs-toggle="modal" data-bs-target="#exampleModal" class="btn btn-secondary mt-4 bt-4">Delete</button>
    </div>
    </section>
    {% include "offer_type/_modalDeleteOffer.html.twig" %}
{% endblock %}
