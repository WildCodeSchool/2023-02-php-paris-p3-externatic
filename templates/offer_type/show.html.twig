{% extends 'base.html.twig' %}

{% block title %}Offer{% endblock %}

{% block body %}
    <h1>Offer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offer.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ offer.title }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ offer.createdAt ? offer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>StartAt</th>
                <td>{{ offer.startAt ? offer.startAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Contract</th>
                <td>{{ offer.contract }}</td>
            </tr>
            <tr>
                <th>WorkFromHome</th>
                <td>{{ offer.workFromHome }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offer.description }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ offer.experience }}</td>
            </tr>
            <tr>
                <th>MinSalary</th>
                <td>{{ offer.minSalary }}</td>
            </tr>
            <tr>
                <th>MaxSalary</th>
                <td>{{ offer.maxSalary }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ offer.location }}</td>
            </tr>
            <tr>
                <th>InterviewProcess</th>
                <td>{{ offer.interviewProcess }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ offer.picture }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ offer.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offer_type_index') }}">back to list</a>

    <a href="{{ path('app_offer_type_edit', {'id': offer.id}) }}">edit</a>

    {{ include('offer_type/_delete_form.html.twig') }}
{% endblock %}
