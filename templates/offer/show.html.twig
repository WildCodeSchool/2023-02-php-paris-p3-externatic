{% extends "base.html.twig" %}

{% block title %}Fiche d'une annonce -
	{{ offer.title }}
{% endblock %}

{% block body %}

	<article>
		<h3>{{ offer.title }}
			<em>{{ offer.createdAt|date('d-m-Y') }}</em>
		</h3>
		<p>{{ offer.description }}</p>
		<a href="{{ path("offer_index") }}">Postuler</a>
	</article>

{% endblock %}
