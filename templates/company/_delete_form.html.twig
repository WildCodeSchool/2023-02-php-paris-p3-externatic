<form method="post" action="{{ path('company_delete', {'id': company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
	<button class="btn btn-primary mt-4 bt-4">Delete</button>
</form>
