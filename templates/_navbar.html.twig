<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-md" id="main-nav">
    <a class="navbar-brand" href="{{ path('home_index') }}">
      <img src="{{ asset('build/images/EXTERNATIC-LOGO.png') }}" alt="" width="200">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">About us</a>
            </li>
            <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Find a job</a>
            </li>
            <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Find a company</a>
            </li>
        </ul>
    </div>
    {% if app.user %}
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <img src="{{ asset(app.user.candidate.picture) }}" id="profile-picture-navbar" alt="profile-picture">
            </a>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('logout') }}">Logout</a></li>
            </ul>
    {% else %}
        <a href="{{ path('login') }}">LOGIN</a>
    {% endif %}
  </div>
</nav>