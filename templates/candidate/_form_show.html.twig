{{ form_start(form) }}
    {{ form_widget(form) }}

    {# <div class="form-group mt-4">{{ form_row(form.pictureFile) }}</div> #}
    <div class="form-group mt-4">{{ form_row(form.resumeFile) }}</div>

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
